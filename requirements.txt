# VidComp runtime dependencies (Python 3.10+; tested on 3.13).
# Pinned for reproducibility. If a wheel is unavailable for your exact Python
# version, install the closest available release (see README troubleshooting).

PySide6==6.8.1.1          # Qt for Python GUI
opencv-python==4.11.0.86  # frame sampling / image processing
numpy==2.2.1              # array math (used by OpenCV / imagehash)
Pillow==11.1.0            # image loading for perceptual hashing
imagehash==4.3.1          # perceptual hashing (pHash) + Hamming distance
send2trash==1.8.3         # Recycle Bin deletion

# --- optional, enable extra capabilities ---------------------------------
pyacoustid==1.3.0         # Chromaprint/AcoustID Python wrapper (M9 audio)
scikit-image==0.25.0      # optional SSIM fallback (ffmpeg lavfi used by default)
humanize==4.11.0          # optional nicer size/time formatting

# External command-line tools are NOT pip packages and must be installed
# separately and added to PATH (see README):
#   * ffmpeg, ffprobe   (https://www.gyan.dev/ffmpeg/builds/)
#   * fpcalc (Chromaprint, https://acoustid.org/chromaprint)
